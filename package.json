{
  "name": "figura-viva-root",
  "private": true,
  "scripts": {
    "dev": "cd next-platform && npm run dev",
    "build": "cd next-platform && npm run build",
    "start": "cd next-platform && npm run start",
    "lint": "cd next-platform && npm run lint"
  }
}